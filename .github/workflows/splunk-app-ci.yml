name: Splunk App CI/CD

on:
  push:
    branches:
      - "**"
    tags:
      - "v*.*.*"
  workflow_dispatch: {}

permissions:
  pull-requests: write
  actions: write
  checks: write
  contents: write

jobs:
  test:
    name: unit-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.9"
      - name: Install test deps
        run: pip install -r requirements-dev.txt
      - name: Run pytest
        run: python -m pytest tests/ -q

  package:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Determine version
        id: version
        shell: bash
        run: |
          SHORT_COMMIT_HASH=$(git rev-parse --short HEAD)
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            TAG_VERSION=${GITHUB_REF#refs/tags/v}
          else
            TAG_VERSION="1.0.0"
          fi
          echo "VERSION_WITH_HASH=${TAG_VERSION}+${SHORT_COMMIT_HASH}" >> $GITHUB_OUTPUT
          echo "TAG_VERSION=${TAG_VERSION}" >> $GITHUB_OUTPUT

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "20"

      - name: Build UI bundle
        working-directory: ui
        run: |
          npm ci
          npm run build

      - name: Stage app
        shell: bash
        run: |
          mkdir -p stage/viz-alert-snapshot
          # Anchored excludes: /ui/ = top-level source only (built bundle already
          # lives under appserver/static/pages and is staged).
          rsync -a \
                --exclude '/.git/' \
                --exclude '/.github/' \
                --exclude '/.gitignore' \
                --exclude '/.gitattributes' \
                --exclude '/ui/' \
                --exclude '/tests/' \
                --exclude '/docs/' \
                --exclude '/dist/' \
                --exclude '/stage/' \
                --exclude '/requirements-dev.txt' \
                --exclude '.pytest_cache/' \
                --exclude '.venv/' \
                --exclude '__pycache__/' \
                --exclude '*.pyc' \
                --exclude '*.tar.gz' \
                --exclude '.DS_Store' \
                ./ stage/viz-alert-snapshot/
          # AppInspect rejects group/other-writable bits: dirs 755, files 644.
          find stage/viz-alert-snapshot -type d -exec chmod 755 {} +
          find stage/viz-alert-snapshot -type f -exec chmod 644 {} +
          ls -la stage/viz-alert-snapshot/

      - name: Update app version from tag
        working-directory: stage/viz-alert-snapshot
        shell: bash
        run: |
          if [ -f default/app.conf ]; then
            sed -i "s/^version = .*/version = ${{ steps.version.outputs.TAG_VERSION }}/g" default/app.conf
          fi
          if [ -f app.manifest ]; then
            sed -i "s/\"version\": \".*\"/\"version\": \"${{ steps.version.outputs.TAG_VERSION }}\"/g" app.manifest
          fi

      - name: Generate package tarball
        shell: bash
        run: |
          mkdir -p dist
          tar -czf dist/viz-alert-snapshot-${{ steps.version.outputs.VERSION_WITH_HASH }}.tar.gz -C stage viz-alert-snapshot
          ls -la dist/

      - name: Upload package artifact
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/

  integration-test:
    name: integration-test (real render on splunk-docker)
    needs: package
    # Dependabot actors get no secrets/egress guarantees; the hermetic unit
    # suite and AppInspect still cover them.
    if: github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Download package
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist

      - name: Unpack staged app
        shell: bash
        run: |
          mkdir -p stage-app
          tar -xzf dist/viz-alert-snapshot-*.tar.gz -C stage-app --strip-components=1
          # The container splunk user (different uid) must read the app and
          # write into it; throwaway copy, never the artefact.
          chmod -R 777 stage-app

      - name: Start Splunk (wait for health)
        working-directory: docker
        env:
          APP_DIR: ${{ github.workspace }}/stage-app
        run: docker compose up -d --wait --wait-timeout 420

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Run live integration tier (real Chromium render)
        env:
          SPLUNK_MGMT_URL: https://127.0.0.1:8089
          SPLUNK_USER: admin
          SPLUNK_PASSWORD: Changeme1!
        run: |
          pip install pytest
          python -m pytest tests/integration -v -ra

      - name: Splunk logs on failure
        if: failure()
        working-directory: docker
        run: docker compose logs --tail 200 splunk || true

      - name: Tear down
        if: always()
        working-directory: docker
        run: docker compose down -v || true

  appinspect:
    name: quality-appinspect
    needs: package
    uses: livehybrid/deploy-splunk-app-action/.github/workflows/appinspect-cli.yml@v1
    with:
      tags: "cloud,future,private_victoria"
    secrets:
      token: "${{ secrets.GITHUB_TOKEN }}"

  quality-appinspect-api:
    name: quality-appinspect-api
    needs:
      - appinspect
    uses: livehybrid/deploy-splunk-app-action/.github/workflows/appinspect-api.yml@v1
    secrets:
      splunkbase_username: ${{ secrets.SPLUNKBASE_USERNAME }}
      splunkbase_password: ${{ secrets.SPLUNKBASE_PASSWORD }}

  publish-gh:
    permissions:
      contents: write
    needs:
      - appinspect
      - integration-test
    uses: livehybrid/deploy-splunk-app-action/.github/workflows/publish.yml@v1
