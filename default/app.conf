[install]
state = enabled

[ui]
is_visible = 0
label = Viz Alert Snapshot

[launcher]
author = Will Searle (LiveHybrid)
description = Render an alert's results as a single Dashboard Studio visualization (PNG, via Splunk's bundled Chromium) and email it.
version = 0.1.0

[package]
id = viz-alert-snapshot
